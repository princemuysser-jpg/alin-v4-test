# تثبيت منصة آلين v4.1.0

## تحديث مشروعك الحالي

لا تستخدم ملف القاعدة النظيفة على مشروعك الحالي.

1. خذ نسخة احتياطية من قاعدة Supabase وملفات الموقع الحالية.
2. افتح SQL Editor في مشروعك الحالي.
3. نفذ مرة واحدة فقط:

   `database/COURIER_REBUILD_V4_1_0.sql`

4. تأكد أن النتيجة الأخيرة تعرض:

   - `assignment_rpc_ready = true`
   - `transition_rpc_ready = true`
   - `courier_workflow_version = 4.1.0`

5. ارفع جميع ملفات النسخة إلى الموقع، وليس ملفات المندوب وحدها.
6. افتح الموقع في نافذة خاصة أولاً، ثم اختبر دورة الطلب كاملة.

الملف يعمل داخل Transaction. عند ظهور خطأ يتراجع التنفيذ ولا يُفترض أن يثبت نصف التحديث. احتفظ برسالة الخطأ كاملة ولا تنفذ ملفات SQL أخرى فوقه.

## اختبار القبول الإجباري

1. أنشئ أو افتح مندوباً فعالاً واربطه بمنطقة الطلب.
2. أنشئ طلب توصيل جديداً.
3. من الإدارة: حوّل الطلب للمندوب.
4. من حساب المندوب: قبول الطلب.
5. اضغط: استلمت الطلب.
6. اضغط: بدء التوصيل.
7. اضغط: تم التسليم واستلام المبلغ.
8. تأكد من ظهور الطلب في المكتملة ومن احتساب مبلغ الطلب وربح المندوب والذمة مرة واحدة.
9. جرّب ضغط زر القبول مرتين بسرعة؛ يجب ألا تتكرر الحركة.
10. جرّب إسناد مندوب من منطقة مختلفة؛ يجب أن يرفض الخادم العملية برسالة واضحة.

## مشروع جديد وفارغ

في مشروع Supabase جديد فقط، نفذ:

`ALIN_V4_CLEAN_PROJECT_MASTER.sql`

بعد ذلك انشر وظائف الحسابات باستخدام:

`نشر_Edge_Functions_Windows.ps1`

وظيفة `admin-assign-order` لم تعد موجودة ولا يجب نشرها؛ التعيين أصبح RPC داخل قاعدة البيانات.

## الملفات التي لا تُستخدم بعد الآن

- أي SQL باسم 4.0.1 أو 4.0.2 خاص بالمندوب.
- أي مجلد `hotfixes` للمندوب.
- أي Edge Function باسم `admin-assign-order`.

## الفحص المحلي

من داخل المجلد، عند توفر Python وNode:

`python tests/verify_clean_release.py`
