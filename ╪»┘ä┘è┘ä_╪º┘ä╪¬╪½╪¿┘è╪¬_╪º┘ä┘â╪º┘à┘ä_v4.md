# تثبيت منصة آلين v4 على مشروع جديد

هذه النسخة **إعادة بناء نظيفة** وليست تحديثاً للقاعدة القديمة. لا تنفذ ملف SQL على مشروعك الحالي، ولا تحذف المشروع القديم.

## 1) إنشاء مشروع Supabase جديد

أنشئ مشروعاً جديداً وفارغاً واحتفظ بالقيم التالية:

- Project Ref
- Project URL
- Anon/Public Key

استخدم المشروع الجديد للاختبار فقط في البداية.

## 2) إنشاء القاعدة النظيفة

افتح SQL Editor في المشروع الجديد، والصق ملف:

`ALIN_V4_CLEAN_PROJECT_MASTER.sql`

ثم اضغط Run مرة واحدة. الملف يتوقف فوراً إذا وجد جداول آلين قديمة مثل `orders` أو `accounts` أو `ledger`، ولا يحاول ترقيعها.

النتيجة النهائية المتوقعة:

- accounts_ready = true
- orders_ready = true
- ledger_ready = true
- settlements_ready = true
- checkout_ready = true
- transition_ready = true
- finance_ready = true
- orders_columns = 69
- base_tables = 35

إذا ظهر خطأ، لا تعِد التشغيل ولا تنفذ أي ملف آخر. احتفظ برسالة الخطأ كاملة.

## 3) نشر Edge Functions على Windows

ثبت Node.js، ثم افتح PowerShell داخل مجلد النسخة ونفذ:

```powershell
npx supabase login
```

بعد تسجيل الدخول نفذ السكربت المرفق:

```powershell
.\نشر_Edge_Functions_Windows.ps1 `
  -ProjectRef "ضع_project_ref" `
  -BootstrapKey "ضع_مفتاحاً_سرياً_طويلاً_وعشوائياً" `
  -AllowedOrigins "https://اسمك.github.io"
```

`AllowedOrigins` هو أصل رابط الموقع فقط، بدون مسار المستودع. يمكن إضافة أكثر من أصل مفصول بفاصلة.

## 4) رفع رابط اختبار منفصل

ارفع ملفات النسخة إلى مستودع أو فرع اختبار منفصل. لا تستبدل موقعك الحالي.

بعد النشر افتح:

`setup-new-project.html`

أدخل Project URL وAnon Key ومفتاح Bootstrap، ثم أنشئ أول مدير. بعدها نزّل `alin-config.js` وارفع الملف الناتج مكان الملف الموجود في جذر المنصة.

بعد نجاح إنشاء المدير الأول:

1. غيّر أو احذف سر `ALIN_BOOTSTRAP_KEY` من أسرار مشروع Supabase.
2. احذف `setup-new-project.html` من الموقع العام.
3. لا ترسل Project URL وAnon Key كبيانات سرية؛ الحماية تعتمد على RLS. لا تشارك Service Role Key نهائياً.

## 5) اختبار القبول قبل نقل البيانات

نفذ الاختبارات بهذا الترتيب:

1. دخول المدير.
2. إنشاء مدرس ومكتبة ومندوب.
3. رفع غلاف وPDF ملزمة ونشرها.
4. إنشاء طلب استلام من المكتبة.
5. تحويل الطلب إلى قيد الطباعة ثم جاهز ثم تم التسليم.
6. التأكد من قيد `ledger` والأرباح والذمة.
7. تسجيل تسوية وعكس تسوية تجريبية.
8. إنشاء منتج وطلب توصيل وإسناده للمندوب.
9. إلغاء طلب منتج والتأكد من رجوع المخزون مرة واحدة.
10. التأكد أن مكتبة غير مرتبطة لا تستطيع فتح PDF.
11. التأكد أن المدرس لا يرى هاتف الطالب أو عنوانه.
12. الضغط المتكرر على تأكيد الطلب يجب ألا ينشئ طلباً مكرراً.

لا تعتمد النسخة للنشر العام إلا إذا نجحت الخطوات كلها بدون تعديل SQL إضافي.

## 6) نقل البيانات لاحقاً

بعد نجاح الاختبار تنقل فقط:

- الحسابات الفعلية
- الملازم والمنتجات
- المكتبات والمندوبين
- مناطق التوصيل
- الإعدادات

لا تنقل `orders` أو `ledger` أو التسويات أو طلبات الاختبار القديمة من المشروع السابق.

## 7) الانتقال للموقع العام

بعد نجاح الاختبار على الرابط المنفصل:

1. خذ نسخة احتياطية من المشروع الجديد.
2. انقل البيانات الأساسية المعتمدة فقط.
3. كرر اختبار قبول مختصر.
4. غيّر رابط الموقع العام إلى المشروع الجديد مرة واحدة.
5. أبقِ المشروع القديم دون حذف إلى أن تتأكد من سلامة التشغيل الفعلي.
